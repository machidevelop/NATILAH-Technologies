#THESIS

Built a runtime and scheduler that receives quantum works and decides where to 
execute them: local sim CPU, GPU or QPU in the cloud.

Then it returns the result along with a standard report reproducible with metrics.

The objective is to reduce the number of failed attempts and time into a useful
result. Make that teams without experts can operate quantum flows with confidence.



