apiVersion: v1
kind: Service
metadata:
  name: {{ include "qstrainer.fullname" . }}
  labels:
    {{- include "qstrainer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "qstrainer.selectorLabels" . | nindent 4 }}
