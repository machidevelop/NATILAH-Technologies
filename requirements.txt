# Q-Strainer core
numpy>=1.24
scikit-learn>=1.3

# GPU telemetry (install on GPU nodes only)
# pynvml>=11.5

# Quantum backend (optional — SA solver works without it)
# dwave-ocean-sdk>=6.0

# Dev / notebook
jupyter>=1.0
ipykernel>=6.0
